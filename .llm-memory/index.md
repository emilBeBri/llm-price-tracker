# index
tags = #moc #index

The agent's memory Zettelkasten. Atomic notes capture observations,
decisions, and learnings from sessions. `raw/` holds external docs the
user has supplied — the agent NEVER writes into raw/.

# topics

## sources & corroboration
- [[openrouter-aggregator-status]] — OpenRouter widens detection, never write
  authority; `-1` sentinel, Decimal scaling, conflict-table split
- [[deferred-structural-prices]] — price history / off-peak / context tiers
  deferred with explicit triggers; research-report claims adopted vs rejected

## alerting & exit codes
- [[alert-tiers-are-not-data-tiers]] — watched vs background drift, `watch.toml`,
  version parsing across vendor id shapes, why a broken source still wins

# orphans
